# PatchPilot vs EPSS - Benchmark Report

**Status:** could not compute metrics.

**Reason:** calendar hold-out window (published_date >= 2025-01-01) has no rows after right-censoring; ingest fresher data or wait until more CVEs leave the window.

Re-run `make ingest` and `make train` to materialise the inputs, then re-run `make eval`.
